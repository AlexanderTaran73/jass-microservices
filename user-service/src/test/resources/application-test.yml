# Test Profile

debug: true

# Spring

spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

  jpa:
    show-sql: true
    generate-ddl: true


server:
  port: 8080

